/**
 * @deprecated Import from `@/lib/pricing` — admin is presentation-only.
 * Re-export kept for backward compatibility with admin UI imports.
 */
export {
  extractPricingFromRaw,
  formatMoney,
  type ExtractedPricing,
} from '@/lib/pricing/provider-pricing-extractor'
