/**
 * Public aggregator repository entry — preserves `@/lib/aggregator/repository` imports.
 * Implementation is split under `./data` (providers, queries, writes, sync, mapping, types, shared).
 */
export * from './data'
