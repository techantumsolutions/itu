# Copy to .env and fill in values (never commit .env).

# --- Catalog (countries / operators / plans): Supabase Postgres via PostgREST ---
# Apply SQL from supabase/catalog_schema.sql (+ optional catalog_seed.sql) in the SQL Editor.
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Dev only: npm run bootstrap:admin — creates/updates super admin (see script defaults)
# ADMIN_BOOTSTRAP_EMAIL=admin@itu.com
# ADMIN_BOOTSTRAP_PASSWORD=

# Redis (optional). If set, enables server-side caching for CMS and catalog.
REDIS_URL=

# Optional cache purge secret (protects POST /api/cache/purge). If empty, endpoint is open.
CACHE_PURGE_SECRET=

# --- DT One (LCR ingestion, /api/dtone/products, LCR v2) ---
# Required for bootstrap + sync when no credentials are stored on the provider row.
# DTONE_BASE_URL is optional; defaults to https://prepaid.dtone.com
DTONE_API_KEY=
DTONE_API_SECRET=
# DTONE_BASE_URL=

# --- Ding Connect — recharge settlement only (app/api/recharge); catalog APIs no longer use Ding ---
DING_API_BASE_URL=https://api.dingconnect.com
DING_API_KEY=
DING_CLIENT_ID=
DING_CLIENT_SECRET=
