# Dependencies (never commit — Next SWC binaries exceed GitHub's 100 MB limit)
node_modules/
.pnp
.pnp.js

# Next.js
.next/
out/

# Build / tooling
dist/
sidecar-dist/
build/
.turbo/

# Env (keep .env.example in git)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env*.local

# Local / host backup artifacts (encrypted dumps, volume tars)
.backups/

# Debug / logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
*.log
scratch/detect-log.jsonl

# OS / editor
.DS_Store
*.pem
.idea/
.vscode/*
!.vscode/extensions.json

# TypeScript
*.tsbuildinfo

# Testing / coverage
coverage/
.nyc_output/

# Vercel
.vercel

# Local support ticket store (JSON)
/data/
# Supabase local setup
supabase-local/

# Docker
docker-data/
volumes/

# VPS deploy state (current/previous SHA + pinned image refs)
.deploy/

package-lock.json
detect-log.jsonl
